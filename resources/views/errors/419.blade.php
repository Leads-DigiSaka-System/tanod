<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>419 — Session Expired — TANOD</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: system-ui, -apple-system, sans-serif; display: flex; align-items: center; justify-content: center; min-height: 100vh; background: #f9fafb; color: #111827; }
        .container { text-align: center; padding: 2rem; }
        .code { font-size: 8rem; font-weight: 800; color: #f59e0b; line-height: 1; }
        .title { font-size: 1.5rem; font-weight: 600; margin-top: 0.75rem; }
        .desc { color: #6b7280; max-width: 28rem; margin: 1rem auto 2rem; line-height: 1.5; }
        .btn { display: inline-block; padding: 0.75rem 2rem; background: #007f3d; color: #fff; border-radius: 0.5rem; text-decoration: none; font-weight: 500; transition: background 0.2s; margin: 0 0.25rem; }
        .btn:hover { background: #005a2e; }
        .btn-secondary { background: #4b5563; }
        .btn-secondary:hover { background: #374151; }
    </style>
</head>
<body>
    <div class="container">
        <div class="code">419</div>
        <div class="title">Session Expired</div>
        <p class="desc">Your session has expired. Please refresh the page and log in again.</p>
        <a href="javascript:location.reload()" class="btn">Refresh Page</a>
        <a href="/login" class="btn btn-secondary">Log In</a>
    </div>
</body>
</html>
